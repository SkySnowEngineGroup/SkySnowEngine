#version 330 core
out vec4 FragColor;
 layout (std140) uniform TestUniformBlock
 {
    vec4 uColor;
    vec4 uColor2;
 };
 uniform vec4 test1;
 uniform vec4 test2;
 uniform vec4 test3;
 uniform sampler2D TestMap_Sampler0;
in vec4 vColor;
in vec2 vUv;
void main()
{
	vec4 utest = test1 + test2 + test3 + uColor + uColor2;
	vec4 testColor = texture(TestMap_Sampler0, vec2(vUv.x,1.0 - vUv.y));
	FragColor = vec4(testColor.xyz,1.0f);
}